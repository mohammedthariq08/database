{
  "name": "database",
  "version": "1.0.0",
  "description": "Mock API using JSON Server",
  "main": "db.json",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}